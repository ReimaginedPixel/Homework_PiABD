
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE PROCEDURE ile_rek2

AS
BEGIN
	SET NOCOUNT ON;
	DECLARE @ile AS int
	SET @ile = (SELECT COUNT(*) FROM Products AS p)
END
GO

EXEC dbo.ile_rek2
